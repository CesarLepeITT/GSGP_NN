name: gsgp_nn
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.12
  - pytorch>=2.0
  - pytorch-cuda=12.1
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - pyyaml
  - pytest
  - pip:
    - tensorboard
